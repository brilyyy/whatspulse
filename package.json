{
  "name": "whatspulse",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:injection": "vite build --config vite.injection.config.ts",
    "build": "tsc && pnpm build:injection && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "distribute": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
		"vite": "^8.0.16",
    "typescript": "~6.0.3"
  }
}
